import './loader.css';

const Loader = () => {
    return (
        <div className="loader" data-testid="loader">
            <div className="loader__spinner"></div>
        </div>
    );
};

export default Loader;
